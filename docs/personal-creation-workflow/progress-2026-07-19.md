# Reading Capture 个人创作工作流开发进度

状态：暂停，等待下一次继续  
分支：`personal/topic-miner`  
记录时间：2026-07-20 00:10（Asia/Shanghai）  
实现依据：本目录设计文档、`personal-topic-miner-creation-workflow-design.md` 与已确认的 `creation-workflow-prototype.html`

## 1. 当前目标

在个人分支中，以 TDD 实现 Reading Capture 的八阶段创作项目工作流，并通过本地 Skill Runner 调用 Codex Skills，跑通微信公众号和小红书两类交付物的生成、审核、视觉制作与不可变发布快照导出。创作项目视图需按批准原型 1:1 还原；通用版 Reading Capture 不得包含个人工作流与 Runner。

## 2. 今天已完成

### 2.1 八阶段工作流与主要交互

- 已建立八阶段状态模型：项目与灵感、素材诊断、研究与证据、主简报审核、平台内容方案、内容审核、视觉验收、定稿与导出。
- 已区分灵感创作和已保存文章改编两种入口；文章改编会跳过第 2–4 步，且来源只在第一步选择一次。
- 微信和小红书作为独立交付物保存自己的阶段、版本、QA 与验收状态。
- 已实现关联灵感解除、人工版本保存、AI 修订、旧 QA 失效、下游任务 supersede、返回研究补证等恢复动作。
- 已实现项目列表、十个以上项目的滚动选择器、八阶段导航和各阶段的主要审核门槛。

### 2.2 Runner 与数据安全

- 队列、Task 引用、运行回执和工作流状态均从 Vault 持久化恢复，不依赖浏览器会话。
- Runner 使用独立 Attempt 工作区；Codex 的 `--cd` 已改为隔离工作区，未声明写入不会进入真实项目。
- 修复了 Obsidian 缓存比外部 Runner 文件落后的问题；任务队列、工作流状态与 Markdown 回执均可从磁盘恢复。
- 并行研究路线改为各自写入 `research/routes/<skill>/`，统一验收时再无损合并，避免 Deep Research 与 Last30Days 相互覆盖。
- 微信写作、计划与 QA 均显式接收研究证据和来源文件。

### 2.3 真实微信公众号路径

真实项目：

```text
Reading Capture/creation-projects/20260719142533_x22f_AI Agent 上线前，至少要做哪五项风险评估
```

已真实完成：

1. 素材诊断。
2. Deep Research Skills 与 Last30Days 两路研究。
3. 研究证据和来源无损合并。
4. 主简报、公众号提纲与配图计划。
5. 多轮正文修订与 Writing Styles QA。
6. QA 分数从 66、82、93 提升到 96，最终任务已通过并完成验收。
7. 已调用两克伴“小小克”配图 Skill，生成 9 张真实 PNG 图片和 `visuals/manifest.md`。

当前真实状态：

- `workflow-state.json` 位于 `visual` 阶段。
- `wechat.visual` 任务 `task_20260719234225_oz42_wechat-visual_liangkeban-xiaoxiaoke-illustrations` 为 `awaiting_approval`。
- 9 张图片均已落盘；已抽查第 1 张和第 9 张，中文、构图和小小克风格正常。
- 尚未在 Obsidian 中完成“逐张检查”确认，也尚未创建最终微信发布快照。

### 2.4 今天最后新增的 TDD 改动

以下改动已经写入工作树，但最后一批尚未重新跑完整回归：

- 小红书第 5 步不再显示硬编码的“结构化评审板”等示例。`xhs.plan` 新增 `proposals.json`，界面只显示 Keke Social Card / Writing Styles 针对真实来源生成的三套方案。
- `proposals.json` 要求恰好三套方案，并验证 `id/name/template/palette/pageCount/pageCountReason/tradeoff/pages` 等字段。
- `xhs.package` 必须读取用户确认后的 `plan-decision.json`，保证整套出图使用确切选中的模板、配色和分页计划。
- 小红书视觉包与文案 QA 低于 95 分时，Runner 会自动进入下一轮；最多五轮，五轮仍未通过则进入 `waiting_user`，不伪装成可验收结果。
- `xhs.copy-qa` 改为可在不修改卡片的前提下迭代修订 `caption.md`，并单独更新 `copy-qa.md`。
- 微信发布快照改为规范结构：`article.md`、`images/`、`sources.md`、`QA.md`、`publishing-notes.md`、`manifest.yaml`。
- 小红书发布快照改为规范结构：`images/`、`BRIEF.md`、`xiaohongshu-caption.md`、`copy-variants.md`、`sources.md`、`QA.md`、`publishing-notes.md`、`manifest.yaml`。
- 新增导出锁和 `autoVersion`，同进程并发导出可分别保留基础目录与 `_v2`，不覆盖已有快照。
- 定稿页可显示最近一次快照，并提供“打开发布目录”和“复制发布路径”。

## 3. 测试状态

最后一次完整通过的命令：

```text
npm test
npm run release:check
```

当时结果：

- reading-core tests passed
- creation workflow tests passed
- plugin capture test passed
- skill runner tests passed
- package succeeded
- release package tests passed

注意：完整通过发生在今天最后一批“小红书双 QA 与规范快照”补充完成之前。随后新增了一个 `testXhsTaskChainUsesApprovedPlanAndIndependentDualQa` 测试函数，但还没有接入 `test/plugin-capture.test.js` 末尾的 Promise 测试链。因此明天第一步必须先把它接入测试链，再跑完整回归，不能把当前工作树直接视为全绿。

## 4. 已安装到真实 Vault 的状态

已将最新打包的以下文件复制到真实 Vault：

```text
.obsidian/plugins/reading-capture/main.js
.obsidian/plugins/reading-capture/styles.css
.obsidian/plugins/reading-capture/manifest.json
```

需要注意：真实 Vault 中 `creation-workflow.js`、`reading-core.js` 和 `skill-runner.js` 的修改时间仍为 23:36；今天最后对 `scripts/skill-runner.js` 的自动迭代改动尚未重新打包并完整安装。明天回归通过后应重新运行打包，再把完整 `dist/reading-capture/` 内容安装到真实插件目录。

## 5. 当前阻塞

- Mac 当前锁屏，Computer Use 返回“automatic unlock could not unlock it”。因此最后的真实 Obsidian 点击验收、截图和 1:1 对照无法继续。
- 这不是代码阻塞。解锁后可从公众号视觉验收页直接续接。

## 6. 明天按顺序继续

1. 将 `testXhsTaskChainUsesApprovedPlanAndIndependentDualQa` 接入 `test/plugin-capture.test.js` 的执行链。
2. 运行该测试并确认先前预期的 TDD 状态；修复失败后执行 `npm test` 和 `npm run release:check`。
3. 检查最后一批改动是否影响通用发布隔离；确认 generic 包中没有 Creation Project、Runner、个人路径和 Skill ID。
4. 重新打包并将完整插件文件安装到真实 Vault，确保 `creation-workflow.js` 与 `skill-runner.js` 也是最新版本。
5. 解锁 Mac 后，在真实 Obsidian 中刷新插件，验收当前微信项目的 9 张图片，进入第 8 步并创建微信不可变发布快照。
6. 检查微信快照是否位于配置的微信目录，目录名为 `YYYYMMDD_主题`，内部结构和图片引用是否正确；测试已有目录冲突时选择 `_v2`。
7. 真实跑通“小红书从灵感/主简报开始”路径：三套真实方案、可选样张比较、完整卡片、视觉 QA、文案 QA、人工验收、最终导出。
8. 真实跑通“小红书从已保存文章改编”路径：第一步选择文件一次，第 5 步复用来源，不再次弹出选择器，完成生成与导出。
9. 验证小红书视觉与文案任一低于 95 分时自动迭代，五轮仍失败时显示 `waiting_user`；确认两个 QA 通道互不冒充通过。
10. 在固定参考视口逐阶段截图，与已批准原型进行并排和像素差异检查；记录所有差异并修正，不以构建成功代替视觉验收。
11. 补齐仍未完成的 Runner 运行保障：设备注册与所有权 epoch、心跳与租约、睡眠/唤醒恢复、三次 Attempt 退避、取消、缺失 Skill 安装与权限扩展提示。
12. 补齐 Artifact/Approval 的稳定 ID、内容哈希与依赖哈希、版本比较与回滚、上游变化后的精确 stale 传播，以及发布后复盘和 Topic Miner feedback 投影。
13. 完成全部真实路径和设计矩阵审计后，再向用户提交验收结果。按 `AGENTS.md`，未获用户验收前不要提交实现 commit。

## 7. 关键文件

- `plugin/main.js`：创作项目 UI、Vault 持久化、任务创建、验收、导出。
- `plugin/creation-workflow.js`：八阶段纯状态模型。
- `scripts/skill-runner.js`：隔离执行、输出校验、质量迭代和回执。
- `plugin/styles.css`：批准原型对应样式。
- `test/plugin-capture.test.js`：插件与视图路径测试。
- `test/creation-workflow.test.js`：工作流状态测试。
- `test/skill-runner.test.js`：Runner 安全和执行测试。
- `docs/personal-creation-workflow/implementation-plan.md`：TDD 分片计划。
- `docs/personal-creation-workflow/path-validation-matrix.md`：真实路径验收矩阵。

## 8. Git 状态与注意事项

- 当前分支：`personal/topic-miner`。
- 没有 staged 文件，也没有为今天的实现创建 commit。
- 工作树包含此前设计文档、实现代码、测试、`.gstack/`、`output/` 和若干图片生成脚本。不要使用 `git add -A`；提交前需逐项区分本功能文件、生成物和用户已有改动。
- 最近提交：`620ea60 require prototype fidelity and TDD`。
- 本次暂停只新增这份进度文档，没有继续执行功能开发。
